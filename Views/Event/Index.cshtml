@model IEnumerable<vps.Models.Event.Event>

<div class="row">
    <button class="btn btn-success" asp-action="Create" asp-controller="Event">Create</button>
    
    <div class="col-12">
        <table class="table table-striped my-5">
            <thead>
            <tr>
                <th>ID</th>
                <th>Title</th>
                <th>Image</th>
                <th>Description</th>
                <th>Location</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var ev in Model)
            {
                <tr>
                    <td>@ev.Id</td>
                    <td>@ev.Title</td>
                    <td>@ev.Img</td>
                    <td>@ev.Description</td>
                    <td>@ev.Location</td>
                    <td>@ev.Status</td>
                    <td>
                        <a class="btn btn-warning" asp-route-id="@ev.Id" asp-action="Edit" asp-controller="Event" >Edit</a>
                        <a class="btn btn-danger" asp-route-id="@ev.Id" asp-action="Delete" asp-controller="Event" >Delete</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>